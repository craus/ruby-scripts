#01
0 is nil
1 is pair nil nil

#bools
true is 1
false is 0
then x is x
else x is x
both x y is if x then y else false
either x y is if x then true else y
not x is if x then false else true
neither x y is if x then false else not y

#chars
char b0 b1 b2 b3 b4 b5 b6 b7 is pair pair pair b0 b1 pair b2 b3 pair pair b4 b5 pair b6 b7 
cin is char bin bin bin bin bin bin bin bin
bout2 x is both bout first x bout second x
bout4 x is both bout2 first x bout2 second x
cout c is both bout4 first c bout4 second c

#ascii
\n is char 0 0 0 0 1 0 1 0
'A' is char 0 1 0 0 0 0 0 1
'B' is char 0 1 0 0 0 0 1 0
'C' is char 0 1 0 0 0 0 1 1

#stacks
[] is nil
push_back stack element is pair stack element 
top stack is second stack
pop stack is first stack
reverse_ stack result is if stack then reverse_ pop stack push_back result top stack else result
reverse stack is reverse_ stack nil

#equation
eq x y is 
  if x 
    if y 
    then both 
      eq first x first y 
      eq second x second y
    else false
  else not y

#strings
linein_ c s is if eq c \n then s else linein_ cin push_back s c 
linein is linein_ cin []
sout stack is if stack then both sout pop stack cout top stack else 1


